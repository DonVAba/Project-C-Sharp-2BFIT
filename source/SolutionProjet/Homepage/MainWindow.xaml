<Window x:Class="Homepage.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Homepage.ucuser"
        mc:Ignorable="d"
        Title="Page d'accueil" MinHeight="500" MinWidth="800" Height="470" Width="800">
    <Window.Resources>
        
    </Window.Resources>
    <DockPanel>
        <StackPanel DockPanel.Dock="Left" Background="CornflowerBlue">
            <Button Click="MWButtonProfile_Click" Background="SkyBlue" BorderBrush="CornflowerBlue" BorderThickness="2">
                <Image Source="Images;Component/img/icon/icon.png" Width="20" Height="35"/>
            </Button>
            <ListBox Width="146" Background="CornflowerBlue" ScrollViewer.VerticalScrollBarVisibility="Visible" Height="412" ItemsSource="{Binding ListProgrammes}" x:Name="listBoxProg"
                     SelectionChanged="listBoxProg_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <WrapPanel Height="60" Width="110" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <WrapPanel.Background>
                                <ImageBrush ImageSource="{Binding CheminImage}"/>
                            </WrapPanel.Background>
                            <TextBlock Text="{Binding Nom}" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" Foreground="Red" FontWeight="Bold"/>
                        </WrapPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
               
            </ListBox>
        </StackPanel>
        <ContentControl x:Name="MainWindowContentControl">
            <local:UserProfilUC Loaded="UserProfilUC_Loaded"/>
        </ContentControl>

    </DockPanel>
</Window>
